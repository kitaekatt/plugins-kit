# docs/reference/orchestrate/ -- orchestration-policy derivation source

Maintainer-only source for the **generated decision half** of the orchestrate
skill's shipped policy. Nothing in this directory ships to a plugin consumer
(see the plugin-opinion razor's OP-1,
[docs/reference/plugin-opinion-razor.md](../plugin-opinion-razor.md)) --
`tier-principles.md` and `orchestrate-2.0-design.md` were deliberately moved
here, out of the published skill tree, in commits `1ba87e2`, `0e08fca`,
`8bc3f9c`, `d6d7aae`, `566c941`. That move also removed the in-skill SKILL.md
links to this machinery, so this file is now the only place that discloses the
chain below.

```yaml
claude_md:
  _schema_version: "1"
  scope:
    directory: docs/reference/orchestrate
    covers:
      - the generation chain that produces the decision half of orchestration.yaml
      - the one-way authorship rule for that chain
      - why the chain's two inputs live in two different roots
      - the retirement of the former fingerprint-based drift guard
    excludes:
      - the shipped orchestrate skill's runtime behavior (covered by plugins/awesome-kit/skills/orchestrate/SKILL.md)
      - the controlled vocabulary itself (covered by plugins/awesome-kit/skills/orchestrate/references/lexicon.md, which DOES ship)
      - general publish/version-bump discipline (covered by root CLAUDE.md)
  insights:
    - id: orchestration_yaml_generation_chain
      keywords:
        - generated orchestration.yaml
        - decision half
        - tier-principles.md
        - generate_orchestration.py
        - hand-edit orchestration.yaml
        - one-way authorship
        - regenerate policy
      summary: >-
        The DECISION half of plugins/awesome-kit/skills/orchestrate/defaults/orchestration.yaml
        (resolution, lexicon, shape, backend, ladders, agent_types, effort, announce)
        is GENERATED by scripts/generate_orchestration.py from the fenced YAML
        principle blocks in tier-principles.md, in the vocabulary of lexicon.md.
        Never hand-edit that half and back-fill a principle to match it -- change
        the principle first, then regenerate.
      detail: |
        Regenerate: `uv run python scripts/generate_orchestration.py --write`.
        Verify: `uv run python scripts/generate_orchestration.py --check` (reads
        staged blobs when anything is staged, so it checks what is about to be
        committed, not just the working tree).

        `--check` is chained from scripts/pre-commit-version-check.sh and is now
        the SOLE commit-time gate on this chain. A prior fingerprint-based guard,
        scripts/check_orchestration_drift.py, was retired as redundant once this
        check existed: a fingerprint cannot disagree with principles the artifact
        is compiled from. Do not describe a fingerprint-drift guard as live, and
        do not reintroduce one without first removing this one -- they would be
        two mechanisms checking the same fact.

        The MACHINE half (backends, capacity) and the file's own header comment
        are hand-maintained and spliced around the generated data untouched;
        only the decision half is off-limits to hand edits.
      origin: >-
        Recorded 2026-08-08 when 1ba87e2/0e08fca/8bc3f9c/d6d7aae/566c941 moved
        this derivation source out of the published skill tree and removed the
        SKILL.md links that used to disclose it in-plugin.
      added: "2026-08-08"
    - id: two_input_roots_deliberate
      keywords:
        - tier-principles.md
        - lexicon.md
        - two roots
        - maintainer-only vs published
        - do not colocate
        - OP-1
      summary: >-
        The generator reads from two inputs in two different repo roots ON
        PURPOSE: tier-principles.md here (maintainer-only, never ships) and
        lexicon.md inside the published skill (ships, because the rendered
        orchestration.yaml tree carries its terms). Do not "tidy" them back
        into one location.
      detail: |
        tier-principles.md carries the criteria plus per-principle `emits:`
        blocks -- the derivation logic itself, which a consumer's plugin cache
        never contains and has no use for. lexicon.md is the controlled
        vocabulary the generated tree's terms are drawn from, and a consumer
        needs it to read their own rendered policy, so it stays inside
        plugins/awesome-kit/skills/orchestrate/references/ where it ships.

        This split is the OP-1 remediation (no maintainer-only material on a
        published surface) applied to this chain -- see
        ../plugin-opinion-razor.md. Someone "simplifying" the two roots into
        one location would either re-ship a maintainer-only file (regressing
        OP-1) or strand the shipped vocabulary outside the skill consumers
        install. Neither is a cleanup.
      origin: >-
        Recorded 2026-08-08 alongside orchestration_yaml_generation_chain, at
        the same disclosure gap left by the unshipping commits.
      added: "2026-08-08"
    - id: reversion_posture
      keywords:
        - reversion
        - reverse the move
        - reinstate a guard
        - re-ship maintainer files
        - collapse the two roots
        - intent record not a prohibition
      summary: >-
        This file records intent as of the 2026-08-08 unshipping move. It is a
        disclosure, not a prohibition -- an agent that later wants to re-ship
        tier-principles.md into the plugin, reinstate a fingerprint-style drift
        guard, or collapse the two input roots into one is free to do so, but
        owns updating (or removing) the affected records in this file as part
        of that change.
      detail: |
        The two-root split, the one-way authorship rule, and the retired-guard
        note above are all decisions in effect right now, not permanent
        invariants. If a future change reverses any of them, this file drifting
        out of sync with reality is the failure this record exists to prevent
        -- so the revert and this file's update belong in the same commit.
      origin: >-
        Stated explicitly per the authoring brief for the 2026-08-08
        disclosure work, to keep this file from reading as an argument to
        preserve against rather than a record of current state.
      added: "2026-08-08"
```

## Model identifiers are validated by dispatch, not by inspection

The machine half of `orchestration.yaml` names model ids and a Codex `-c`
config key that agents hand straight to a CLI. Neither is checkable by reading:
a bare codename (`sol`) has the same shape as a fully qualified id, and
`model_reasoning_effort` is a config key `codex exec --help` does not list.

`scripts/check_model_dispatch.py` validates them for real -- one trivial prompt
per rung model, plus a zero-token `--strict-config` launch per config key. Run
it whenever a ladder rung, a model id, or a dispatch flag changes:

    uv run python scripts/check_model_dispatch.py
    uv run python scripts/check_model_dispatch.py --list      # dispatches nothing

Nothing runs it automatically: it is out of the pytest suite (network, cost),
out of the pre-commit chain, and out of `publish.py`'s preflight, so a retired
model id can reach consumers if nobody runs it. The offline suite's
`TestNoBareCodenames` checks shape only and points here.

## Cross-references

- Design/derivation record for this chain: `orchestrate-2.0-design.md` (this
  directory).
- Criteria + `emits:` blocks the generator compiles: `tier-principles.md` (this
  directory).
- Published vocabulary the generator also reads:
  `../../../plugins/awesome-kit/skills/orchestrate/references/lexicon.md`.
- Generator: `../../../scripts/generate_orchestration.py`.
- OP-1 rationale for the maintainer/published split:
  `../plugin-opinion-razor.md`.
