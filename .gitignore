__pycache__/
*.pyc
*.pyo
.env
.venv/
*.egg-info/
dist/
build/
.claude/bootstrap.local.json
.claude/settings.local.json
uv.lock
tmp/
.claude/p4-kit.yaml
.local-data/
CLAUDE.local.md
.claude/workflows/
.workflow-kit/
.githooks/project-terms.txt
.DS_Store
plugins/unreal-kit/skills/ue-python-api/stubs/unreal.py
# dev/tasks is a directory junction (Windows) / symlink into the PRIVATE
# kitaekatt/tasks repo, where this project's task folders live and are
# version-controlled -- see "Task folders live in a private tasks repo" in
# CLAUDE.md. This entry is load-bearing: it is what stops git traversing the
# link and staging private task content into this PUBLIC repo. Do not add a
# !dev/tasks exception.
dev/tasks/

# Synthetic dev-layout registry written by scripts/claude_plugin_test.py
# (session-scoped; never committed -- see that script for why it lives here)
plugins/installed_plugins.json
