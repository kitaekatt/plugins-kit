diff --git a/src/clamp.py b/src/clamp.py
new file mode 100644
--- /dev/null
+++ b/src/clamp.py
@@ -0,0 +1,4 @@
+def clamp(value: int, low: int, high: int) -> int:
+    if value < low
+        return low
+    return min(value, high)
