type: Graph
components:
  name:
    name: hello
  description:
    description: Minimal sample graph for loader round-trip and pre-commit acceptance.
  start:
    node: greet
  state_decl:
    init:
      caller: str
    accumulated:
      audit: "list[AuditRecord]"
  version:
    value: 1
  config:
    settings:
      max_parallel: 4
      show_work: true
