# c1 fixture directory

Classic CLAUDE.md carrying a valid claude_md contract block.

```yaml
claude_md:
  _schema_version: "1"
  scope:
    directory: fixtures/c1
    covers:
      - fixture conventions
  insights:
    - id: fixture_insight
      keywords: [fixture, insight, corpus]
      summary: The fixture project stores widgets in batches.
      detail: Batches are the unit of allocation; see the allocator notes.
      origin: Golden-corpus fixture authoring.
      added: "2026-07-28"
```
