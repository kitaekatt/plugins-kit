# c4 fixture directory

Conventions-only CLAUDE.md: valid under the union floor (no insights needed).

```yaml
claude_md:
  _schema_version: "1"
  scope:
    directory: fixtures/c4
    covers:
      - fixture conventions
  conventions:
    - rule: Always allocate widgets in batches.
      keywords: [widgets, batches, allocation]
      why: Single-widget allocation bypasses the limit check.
```
