{
  "$comment": "awesome-kit depends on plugins-kit:skills-kit for the plugin-ecosystem audit framework and skills_kit_lib, and dispatch.py requires bootstrap_lib.codex for launcher resolution and the cmd-metacharacter refusal, while the renderer consumes llm_scripting_kit plus bootstrap_lib optionally for harness model definitions and presence detection. Auto-install skills-kit when missing. requires_bootstrap is set from the newest bootstrap_lib call this plugin makes -- dispatch.py's bootstrap_lib.codex.build_codex_exec_argv, which first shipped (alongside the module) in bootstrap 0.79.0 -- not from the module import alone.",
  "requires_bootstrap": "0.79.0",
  "plugins": [
    {
      "ref": "plugins-kit:skills-kit",
      "enabled": true,
      "scope": "user",
      "min_version": "0.12.0",
      "install": "auto"
    }
  ],
  "venv": {
    "check_imports": ["yaml"]
  },
  "shared_lib_imports": ["skills_kit_lib", "llm_scripting_kit", "bootstrap_lib"]
}
