{
  "$comment": "awesome-kit depends on plugins-kit:skills-kit for the plugin-ecosystem audit framework and skills_kit_lib, and dispatch.py requires bootstrap_lib.codex for launcher resolution and the cmd-metacharacter refusal, while the renderer consumes llm_scripting_kit plus bootstrap_lib optionally for harness model definitions and presence detection. Auto-install skills-kit when missing. requires_bootstrap is set from the newest bootstrap call this plugin makes: the plugin-ecosystem skill launches generate.py as \"${BOOTSTRAP_PYTHON:?requires bootstrap >= 0.120.0}\", which only bootstrap 0.120.0's SessionStart hook exports (the newest bootstrap_lib call, dispatch.py's bootstrap_lib.codex.build_codex_exec_argv, first shipped in 0.79.0).",
  "requires_bootstrap": "0.120.0",
  "plugins": [
    {
      "ref": "plugins-kit:skills-kit",
      "enabled": true,
      "scope": "user",
      "min_version": "0.12.0",
      "install": "auto"
    }
  ],
  "venv": {
    "check_imports": ["yaml"]
  },
  "shared_lib_imports": ["skills_kit_lib", "llm_scripting_kit", "bootstrap_lib"]
}
