{
  "$comment": "awesome-kit's claude-explorer skill references the audit framework hosted in plugins-kit:skills-kit (md-audit/references/audit-framework.{md,yaml}). Auto-install when missing. The html-pdf skill needs Playwright (venv) + a Chromium browser, installed by custom_bootstrap.py.",
  "plugins": [
    {
      "ref": "plugins-kit:skills-kit",
      "enabled": true,
      "scope": "user",
      "min_version": "0.12.0",
      "install": "auto"
    }
  ],
  "venv": {
    "check_imports": ["playwright", "pypdf", "yaml"]
  },
  "shared_lib_imports": ["skills_kit_lib"],
  "script": {
    "path": "custom_bootstrap.py",
    "entry_point": "bootstrap"
  }
}
