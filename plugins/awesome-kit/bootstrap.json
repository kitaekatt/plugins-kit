{
  "$comment": "awesome-kit depends on plugins-kit:skills-kit for the plugin-ecosystem audit framework and skills_kit_lib, and orchestrate optionally consumes llm_scripting_kit plus bootstrap_lib for harness model definitions and presence detection. Auto-install skills-kit when missing. The html-pdf skill needs Playwright (venv) + a Chromium browser, installed by custom_bootstrap.py.",
  "plugins": [
    {
      "ref": "plugins-kit:skills-kit",
      "enabled": true,
      "scope": "user",
      "min_version": "0.12.0",
      "install": "auto"
    }
  ],
  "venv": {
    "check_imports": ["playwright", "pypdf", "yaml"]
  },
  "shared_lib_imports": ["skills_kit_lib", "llm_scripting_kit", "bootstrap_lib"],
  "script": {
    "path": "custom_bootstrap.py",
    "entry_point": "bootstrap"
  }
}
