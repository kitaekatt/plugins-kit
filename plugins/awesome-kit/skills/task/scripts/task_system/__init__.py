"""task_system -- the task-system implementation package.

Step 1 ships the typed-unit schemas (schemas.py), the Task Type registry
(types.py), reference resolution (resolve.py), and the `validate` verb
(validate.py). Later steps add the remaining verbs.

Spec (the implementation contract):
plugins/awesome-kit/skills/task/design/task-system-design.md
"""
