{
  "$comment": "prototypes' claude-explorer skill references the audit framework hosted in plugins-kit:skills-kit (md-audit/references/audit-framework.{md,yaml}); auto-install skills-kit when missing. The crawl uses PyYAML from prototypes' OWN venv (declared below + pyproject.toml) -- it must not reach into another plugin's venv. Without the venv the script degrades to regex frontmatter parsing.",
  "plugins": [
    {
      "ref": "plugins-kit:skills-kit",
      "enabled": true,
      "scope": "user",
      "min_version": "0.12.0",
      "install": "auto"
    }
  ],
  "venv": {
    "check_imports": ["yaml"]
  }
}
