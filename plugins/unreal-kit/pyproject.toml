[project]
name = "unreal-kit"
version = "0.11.7"
description = "Host-side dependencies for unreal-kit plugin"
requires-python = ">=3.12,!=3.14.*"

dependencies = [
    "upyrc",
    "pyyaml",
    "websocket-client",
]
