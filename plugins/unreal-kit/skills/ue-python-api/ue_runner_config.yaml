# UE Python Script Runner — global defaults
#
# Project-specific paths (engine_dir, uproject) go in:
#   <project_root>/.local-data/plugins-kit/unreal-kit/config.yaml  (per-project, preferred)
#   <project_root>/.local-data/unreal-kit/config.yaml  (legacy, auto-migrated by bootstrap)
#   <project_root>/.claude/unreal-kit.yaml             (older legacy, auto-migrated by bootstrap)
#   ~/.claude/plugins/data/plugins-kit/unreal-kit/config.yaml  (global, legacy fallback)
#
# Resolution order: CLI args → per-project config → global config → this file → hardcoded defaults

remote_execution:
  multicast_group: "239.0.0.1"
  multicast_port: 6766
