[project]
name = "pdf-kit"
version = "0.1.1"
description = "Runtime dependencies for pdf-kit: render HTML to PDF via headless Chromium."
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40",
    "pypdf>=4.0",
]

[tool.uv]
package = false
