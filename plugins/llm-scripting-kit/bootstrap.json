{
  "$comment": "The venv exists for the CLI, not for the library: models.resolve_endpoint() reads layered config through bootstrap_lib + PyYAML, and OpenAI-compatible completion commands require the OpenAI SDK. The standalone Python carries only .pth links and no third-party packages. bootstrap_lib is linked in because config_resolve lives there.",
  "shared_libs": [
    { "name": "llm_scripting_kit", "src": "lib" }
  ],
  "shared_lib_imports": ["bootstrap_lib"],
  "venv": {
    "extras": ["sdk"],
    "check_imports": ["openai", "yaml"]
  },
  "config": {
    "file": "config.yaml",
    "defaults_source": "lib/llm_scripting_kit/default_config.yaml"
  },
  "script": {
    "path": "custom_bootstrap.py",
    "entry_point": "bootstrap"
  }
}
