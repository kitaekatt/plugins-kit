{
  "name": "llm-scripting-kit",
  "version": "0.9.1",
  "description": "LLM access for scripts and pipelines: key resolution, model registry, named OpenAI-compatible endpoints (OpenRouter is the default), and a completion seam with three backends behind one complete() -- OpenRouter over HTTP plus the local claude-cli and codex-cli transports, both subscription-billed. CLI + shared library + session-start check.",
  "author": {
    "name": "Christina"
  },
  "dependencies": [
    "bootstrap"
  ],
  "keywords": [
    "openrouter",
    "openai",
    "llm",
    "api-key",
    "endpoints",
    "credentials",
    "bootstrap"
  ]
}
